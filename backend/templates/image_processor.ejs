<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Nexus AI - Image Processing Studio</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="{{ url_for('static', filename='css/responsive.css') }}">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: #0a0a0a;
      color: #ffffff;
      overflow-x: hidden;
    }

    .gradient-bg {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .glass-effect {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .image-preview {
      background: repeating-conic-gradient(#1a1a1a 0% 25%, transparent 0% 50%) 50% / 20px 20px;
    }

    .filter-btn {
      transition: all 0.3s ease;
      border: 2px solid transparent;
    }

    .filter-btn:hover {
      border-color: #10b981;
      transform: translateY(-2px);
    }

    .filter-btn.active {
      border-color: #10b981;
      background: rgba(16, 185, 129, 0.1);
    }

    .drop-zone {
      border: 2px dashed #374151;
      transition: all 0.3s ease;
    }

    .drop-zone.dragover {
      border-color: #10b981;
      background: rgba(16, 185, 129, 0.05);
    }

    .loading-spinner {
      border: 3px solid rgba(255, 255, 255, 0.1);
      border-top: 3px solid #10b981;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      animation: spin 1s linear infinite;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    .feature-card {
      transition: all 0.3s ease;
    }

    .feature-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    }
  </style>
</head>

<body>
  <!-- Navigation -->
  <nav class="fixed top-0 left-0 right-0 z-50 glass-effect">
    <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 bg-gradient-to-r from-emerald-500 to-teal-600 rounded-lg flex items-center justify-center">
          <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
          </svg>
        </div>
        <h1 class="text-xl font-bold">Nexus AI</h1>
      </div>
      <div class="flex items-center gap-6">
        <a href="{{ url_for('home') }}" class="text-gray-300 hover:text-white transition-colors">Home</a>
        <a href="{{ url_for('customize_chatbot') }}" class="text-gray-300 hover:text-white transition-colors">Chatbot</a>
        <a href="{{ url_for('customize_upload') }}" class="text-gray-300 hover:text-white transition-colors">Data Analysis</a>
        <a href="{{ url_for('image_processor') }}" class="text-emerald-400 font-semibold">Image Processing</a>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="min-h-screen flex items-center justify-center pt-20 px-6">
    <div class="max-w-6xl mx-auto text-center">
      <div class="mb-8">
        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass-effect text-emerald-400 text-sm font-medium">
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
            <path d="M13 7H7v6h6V7z"></path>
            <path fill-rule="evenodd" d="M7 2a1 1 0 012 0v1h2V2a1 1 0 112 0v1h2a2 2 0 012 2v2h1a1 1 0 110 2h-1v2h1a1 1 0 110 2h-1v2a2 2 0 01-2 2h-2v1a1 1 0 11-2 0v-1H9v1a1 1 0 11-2 0v-1H5a2 2 0 01-2-2v-2H2a1 1 0 110-2h1V9H2a1 1 0 010-2h1V5a2 2 0 012-2h2V2z" clip-rule="evenodd"></path>
          </svg>
          Powered by Python • No API Required
        </div>
      </div>
      <h1 class="text-5xl md:text-7xl font-bold mb-6 bg-gradient-to-r from-emerald-400 to-teal-600 bg-clip-text text-transparent">
        Image Processing Studio
      </h1>
      <p class="text-xl text-gray-300 mb-12 max-w-3xl mx-auto">
        Transform your images with advanced AI-powered processing. Apply filters, detect objects, enhance photos, and extract insights - all running locally on your device.
      </p>
      
      <!-- Quick Stats -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-12">
        <div class="glass-effect rounded-xl p-6">
          <div class="text-3xl font-bold text-emerald-400">50+</div>
          <div class="text-gray-400">Filters & Effects</div>
        </div>
        <div class="glass-effect rounded-xl p-6">
          <div class="text-3xl font-bold text-emerald-400">100%</div>
          <div class="text-gray-400">Privacy</div>
        </div>
        <div class="glass-effect rounded-xl p-6">
          <div class="text-3xl font-bold text-emerald-400">0ms</div>
          <div class="text-gray-400">API Latency</div>
        </div>
        <div class="glass-effect rounded-xl p-6">
          <div class="text-3xl font-bold text-emerald-400">∞</div>
          <div class="text-gray-400">Processing</div>
        </div>
      </div>

      <button onclick="scrollToEditor()" class="bg-gradient-to-r from-emerald-500 to-teal-600 text-white font-bold py-4 px-8 rounded-xl hover:opacity-90 transition-opacity transform hover:scale-105 duration-300">
        Start Processing Images
      </button>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-20 px-6">
    <div class="max-w-6xl mx-auto">
      <h2 class="text-4xl font-bold text-center mb-16">Powerful Features</h2>
      <div class="grid md:grid-cols-3 gap-8">
        <div class="feature-card glass-effect rounded-xl p-8">
          <div class="w-12 h-12 bg-emerald-500/20 rounded-lg flex items-center justify-center mb-6">
            <svg class="w-6 h-6 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold mb-4">Smart Filters</h3>
          <p class="text-gray-400">Apply AI-powered filters, blur, sharpen, and artistic effects to transform your images.</p>
        </div>
        
        <div class="feature-card glass-effect rounded-xl p-8">
          <div class="w-12 h-12 bg-emerald-500/20 rounded-lg flex items-center justify-center mb-6">
            <svg class="w-6 h-6 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold mb-4">Object Detection</h3>
          <p class="text-gray-400">Automatically detect faces, objects, and text in your images using computer vision.</p>
        </div>
        
        <div class="feature-card glass-effect rounded-xl p-8">
          <div class="w-12 h-12 bg-emerald-500/20 rounded-lg flex items-center justify-center mb-6">
            <svg class="w-6 h-6 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold mb-4">Batch Processing</h3>
          <p class="text-gray-400">Process multiple images at once with the same filters and settings for maximum efficiency.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Image Editor Section -->
  <section id="editor" class="py-20 px-6">
    <div class="max-w-6xl mx-auto">
      <h2 class="text-4xl font-bold text-center mb-16">Image Editor</h2>
      
      <div class="grid lg:grid-cols-2 gap-8">
        <!-- Upload Area -->
        <div>
          <div class="glass-effect rounded-xl p-8">
            <h3 class="text-xl font-bold mb-6">Upload Image</h3>
            <div id="dropZone" class="drop-zone rounded-xl p-12 text-center cursor-pointer">
              <svg class="w-16 h-16 text-gray-400 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
              </svg>
              <p class="text-gray-400 mb-2">Drag & drop your image here</p>
              <p class="text-gray-500 text-sm">or click to browse</p>
              <input type="file" id="imageInput" accept="image/*" class="hidden">
            </div>
            
            <!-- Image Info -->
            <div id="imageInfo" class="hidden mt-6 p-4 bg-black/50 rounded-lg">
              <div class="grid grid-cols-2 gap-4 text-sm">
                <div>
                  <span class="text-gray-400">Name:</span>
                  <span id="imageName" class="text-white ml-2"></span>
                </div>
                <div>
                  <span class="text-gray-400">Size:</span>
                  <span id="imageSize" class="text-white ml-2"></span>
                </div>
                <div>
                  <span class="text-gray-400">Dimensions:</span>
                  <span id="imageDimensions" class="text-white ml-2"></span>
                </div>
                <div>
                  <span class="text-gray-400">Format:</span>
                  <span id="imageFormat" class="text-white ml-2"></span>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Preview Area -->
        <div>
          <div class="glass-effect rounded-xl p-8">
            <h3 class="text-xl font-bold mb-6">Preview</h3>
            <div id="previewArea" class="image-preview rounded-xl min-h-[400px] flex items-center justify-center">
              <div class="text-center text-gray-400">
                <svg class="w-16 h-16 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
                <p>Your processed image will appear here</p>
              </div>
            </div>
            
            <!-- Action Buttons -->
            <div id="actionButtons" class="hidden mt-6 flex gap-4">
              <button onclick="resetImage()" class="flex-1 bg-gray-700 text-white py-3 px-6 rounded-lg hover:bg-gray-600 transition-colors">
                Reset
              </button>
              <button onclick="downloadImage()" class="flex-1 bg-emerald-600 text-white py-3 px-6 rounded-lg hover:bg-emerald-700 transition-colors">
                Download
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- Filters Section -->
      <div class="mt-12 glass-effect rounded-xl p-8">
        <h3 class="text-xl font-bold mb-6">Filters & Effects</h3>
        <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('original')">
            <div class="text-2xl mb-2">🎨</div>
            <div class="text-sm">Original</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('grayscale')">
            <div class="text-2xl mb-2">⚫</div>
            <div class="text-sm">Grayscale</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('blur')">
            <div class="text-2xl mb-2">💨</div>
            <div class="text-sm">Blur</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('sharpen')">
            <div class="text-2xl mb-2">✨</div>
            <div class="text-sm">Sharpen</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('brightness')">
            <div class="text-2xl mb-2">☀️</div>
            <div class="text-sm">Brightness</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('contrast')">
            <div class="text-2xl mb-2">🔲</div>
            <div class="text-sm">Contrast</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('sepia')">
            <div class="text-2xl mb-2">🟤</div>
            <div class="text-sm">Sepia</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('invert')">
            <div class="text-2xl mb-2">🔄</div>
            <div class="text-sm">Invert</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('edge')">
            <div class="text-2xl mb-2">📐</div>
            <div class="text-sm">Edge Detect</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="applyFilter('emboss')">
            <div class="text-2xl mb-2">🗿</div>
            <div class="text-sm">Emboss</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="detectFaces()">
            <div class="text-2xl mb-2">👤</div>
            <div class="text-sm">Face Detect</div>
          </button>
          <button class="filter-btn glass-effect rounded-lg p-4 text-center" onclick="extractText()">
            <div class="text-2xl mb-2">📝</div>
            <div class="text-sm">Extract Text</div>
          </button>
        </div>
      </div>
    </div>
  </section>

  <script>
    let currentImage = null;
    let originalImageData = null;

    // Initialize
    document.addEventListener('DOMContentLoaded', function() {
      setupDropZone();
      setupImageInput();
    });

    function setupDropZone() {
      const dropZone = document.getElementById('dropZone');
      
      dropZone.addEventListener('click', () => {
        document.getElementById('imageInput').click();
      });

      dropZone.addEventListener('dragover', (e) => {
        e.preventDefault();
        dropZone.classList.add('dragover');
      });

      dropZone.addEventListener('dragleave', () => {
        dropZone.classList.remove('dragover');
      });

      dropZone.addEventListener('drop', (e) => {
        e.preventDefault();
        dropZone.classList.remove('dragover');
        
        const files = e.dataTransfer.files;
        if (files.length > 0) {
          handleImageUpload(files[0]);
        }
      });
    }

    function setupImageInput() {
      const imageInput = document.getElementById('imageInput');
      imageInput.addEventListener('change', (e) => {
        if (e.target.files.length > 0) {
          handleImageUpload(e.target.files[0]);
        }
      });
    }

    function handleImageUpload(file) {
      if (!file.type.startsWith('image/')) {
        alert('Please upload an image file');
        return;
      }

      const reader = new FileReader();
      reader.onload = (e) => {
        const img = new Image();
        img.onload = () => {
          currentImage = img;
          originalImageData = e.target.result;
          displayImage(e.target.result);
          showImageInfo(file, img);
          showActionButtons();
        };
        img.src = e.target.result;
      };
      reader.readAsDataURL(file);
    }

    function displayImage(imageSrc) {
      const previewArea = document.getElementById('previewArea');
      previewArea.innerHTML = `<img src="${imageSrc}" alt="Preview" class="max-w-full max-h-[500px] rounded-lg mx-auto">`;
    }

    function showImageInfo(file, img) {
      document.getElementById('imageInfo').classList.remove('hidden');
      document.getElementById('imageName').textContent = file.name;
      document.getElementById('imageSize').textContent = (file.size / 1024).toFixed(2) + ' KB';
      document.getElementById('imageDimensions').textContent = `${img.width} × ${img.height}`;
      document.getElementById('imageFormat').textContent = file.type.split('/')[1].toUpperCase();
    }

    function showActionButtons() {
      document.getElementById('actionButtons').classList.remove('hidden');
    }

    function applyFilter(filterType) {
      if (!currentImage) {
        alert('Please upload an image first');
        return;
      }

      // Show loading state
      const previewArea = document.getElementById('previewArea');
      previewArea.innerHTML = '<div class="loading-spinner mx-auto"></div>';

      // Call backend API
      fetch('/api/apply_filter', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          image: originalImageData,
          filter: filterType
        })
      })
      .then(response => response.json())
      .then(data => {
        if (data.success) {
          displayImage('data:image/png;base64,' + data.image);
          
          // Update active button
          document.querySelectorAll('.filter-btn').forEach(btn => btn.classList.remove('active'));
          // Find the button that was clicked
          const clickedBtn = document.querySelector(`[onclick="applyFilter('${filterType}')"]`);
          if (clickedBtn) {
            clickedBtn.classList.add('active');
          }
        } else {
          alert('Error applying filter: ' + data.error);
          displayImage(originalImageData);
        }
      })
      .catch(error => {
        console.error('Error:', error);
        alert('Error applying filter');
        displayImage(originalImageData);
      });
    }

    function detectFaces() {
      if (!currentImage) {
        alert('Please upload an image first');
        return;
      }
      
      // Show loading state
      const previewArea = document.getElementById('previewArea');
      previewArea.innerHTML = '<div class="loading-spinner mx-auto"></div>';

      // Call backend API
      fetch('/api/detect_faces', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          image: originalImageData
        })
      })
      .then(response => response.json())
      .then(data => {
        if (data.success) {
          displayImage('data:image/png;base64,' + data.image);
          alert(`Found ${data.faces_found} face(s) in the image!`);
        } else {
          alert('Error detecting faces: ' + data.error);
          displayImage(originalImageData);
        }
      })
      .catch(error => {
        console.error('Error:', error);
        alert('Error detecting faces');
        displayImage(originalImageData);
      });
    }

    function extractText() {
      if (!currentImage) {
        alert('Please upload an image first');
        return;
      }
      
      alert('Text extraction feature coming soon! This will extract readable text from your image using OCR.');
    }

    function resetImage() {
      if (originalImageData) {
        displayImage(originalImageData);
        document.querySelectorAll('.filter-btn').forEach(btn => btn.classList.remove('active'));
      }
    }

    function downloadImage() {
      if (!currentImage) {
        alert('No image to download');
        return;
      }

      const link = document.createElement('a');
      link.download = 'processed-image.png';
      link.href = originalImageData;
      link.click();
    }

    function scrollToEditor() {
      document.getElementById('editor').scrollIntoView({ behavior: 'smooth' });
    }
  </script>
</body>

</html>
